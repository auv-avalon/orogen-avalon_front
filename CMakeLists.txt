PROJECT(avalon_front)
cmake_minimum_required(VERSION 2.6)

FIND_PACKAGE(Qt4 COMPONENTS QtCore REQUIRED )
INCLUDE( ${QT_USE_FILE} )

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(avalon_frontBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

